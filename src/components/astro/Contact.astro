---
import Button from "../svelte/Button.svelte";

interface Props {
  dark?: boolean;
}

const { dark } = Astro.props;
---

<form class="grid gap-x-16 gap-y-8 md:grid-cols-3">
  <div class="grid gap-1">
    <label
      for="name"
      class="text-sm font-bold uppercase text-dark-700 after:content-[attr(data-after)]"
      class:list={{
        "text-dark-50": dark,
      }}
      data-after="*">Name</label
    >
    <input
      type="text"
      name="name"
      id="name"
      class="rounded-md border border-dark-800 p-2 focus-within:outline-2 focus-within:outline-primary-500"
    />
  </div>
  <div class="grid gap-1">
    <label
      for="email"
      class="text-sm font-bold uppercase text-dark-700 after:content-[attr(data-after)]"
      class:list={{
        "text-dark-50": dark,
      }}
      data-after="*">Email</label
    >
    <input
      type="email"
      name="email"
      id="email"
      class="rounded-md border border-dark-800 p-2 focus-within:outline-2 focus-within:outline-primary-500"
    />
  </div>
  <div class="grid gap-1">
    <label
      for="phone"
      class="text-sm font-bold uppercase text-dark-700 after:content-[attr(data-after)]"
      class:list={{
        "text-dark-50": dark,
      }}
      data-after="*">Phone</label
    >
    <input
      type="tel"
      name="phone"
      id="phone"
      class="rounded-md border border-dark-800 p-2 focus-within:outline-2 focus-within:outline-primary-500"
    />
  </div>
  <div class="grid gap-1 md:col-span-3">
    <label
      for="message"
      class="text-sm font-bold uppercase text-dark-700 after:content-[attr(data-after)]"
      class:list={{
        "text-dark-50": dark,
      }}
      data-after="*">Message</label
    >
    <textarea
      name="message"
      id="message"
      rows="5"
      class="w-full rounded-md border border-dark-800 p-2 focus-within:outline-2 focus-within:outline-primary-500"
    ></textarea>
  </div>
  <Button color="accent">Send message</Button>
</form>
